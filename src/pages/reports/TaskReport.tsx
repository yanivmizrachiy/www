import { SafePage } from "@/components/SafePage"; import { useTaskCompletionDetail } from "@/hooks/useImports"; export default function TaskReport(){ const {data, loading}=useTaskCompletionDetail(); return <SafePage title="דוח משימות" description="דוח completion ממשימות אמיתיות בלבד.">{loading?"טוען...":<pre className="whitespace-pre-wrap text-xs">{JSON.stringify(data ?? {}, null, 2)}</pre>}</SafePage>; }
