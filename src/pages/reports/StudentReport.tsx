import { SafePage } from "@/components/SafePage"; import { useStudentReports } from "@/hooks/useImports"; export default function StudentReport(){ const {data, loading}=useStudentReports(); return <SafePage title="דוח תלמידים" description="דוח תלמידים מנתוני אמת בלבד.">{loading?"טוען...":<pre className="whitespace-pre-wrap text-xs">{JSON.stringify(data ?? [], null, 2)}</pre>}</SafePage>; }
