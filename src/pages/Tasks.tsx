import { SafePage, EmptyTruth } from "@/components/SafePage";
import { useCourseStructure } from "@/hooks/useImports";
export default function Page() { const {data, loading, error} = useCourseStructure(); return <SafePage title="משימות" description="משימות והשלמות לפי דוחות Moodle אמיתיים.">{loading ? <p className="text-sm text-muted-foreground">טוען...</p> : error ? <EmptyTruth>{error}</EmptyTruth> : <ul className="space-y-2">{data?.tasks?.map((t)=><li key={t.id} className='rounded border p-3'>{t.task_name}<div className='text-xs text-muted-foreground'>{t.task_type ?? '—'}</div></li>)}</ul>}</SafePage>; }
