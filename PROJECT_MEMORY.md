# PROJECT MEMORY — Moodle Admin / Teacher Hub / Guide Center

עודכן: 2026-07-08  
ריפו מקור: `yanivmizrachiy/www`  
Runtime קנוני נוכחי: `https://www-tijc.onrender.com`  
Teacher Release: **NO**

הקובץ הזה הוא קובץ הזיכרון והעקרונות המרכזי של הדרישות המתפתחות לפרויקט.  
בכל דרישה חדשה: קודם מעדכנים את הקובץ הזה, אחר כך מתכננים, ורק אחר כך משנים קוד.

---

## מטרת־על

לבנות מערכת מרכזית אחת סביב Moodle של משרד החינוך, עם שני מוצרים נפרדים וברורים:

1. **מצגת/מדריך אינטרנטי חי למורים** — קישור לאתר שפועל כמו מצגת, מתעדכן מהאתר שלי, כולל צילומים אמיתיים והסברים פשוטים.
2. **Moodle Teacher Hub / WWW** — כלי חיצוני שמורה מוסיף למרחב Moodle שלו, מזהה את המורה והמרחב דרך Moodle context, ושולף/מציג נתונים אמיתיים במקסימום האפשרי.

שני המוצרים יכולים להיות באותו אתר, אבל אסור לבלבל ביניהם.

---

## הפרדה מוחלטת בין שני חלקי המערכת

### חלק א׳ — מרכז מצגת והדרכה למורים

זהו אתר אינטרנט שפועל כמו מצגת חיה.  
המצגת אינה PowerPoint, אינה PDF, ואינה קובץ להורדה.  
המורים מקבלים קישור, והאתר שלי שולט בתוכן שלה.

עקרונות:

- קישור חי אחד למצגת.
- חיצים קדימה/אחורה.
- תוכן עניינים.
- ראשי פרקים.
- עברית פשוטה מאוד.
- RTL מלא.
- צילומים אמיתיים מתוך מרחבי Moodle.
- כל שקופית עונה על שאלה אחת או פעולה אחת.
- המצגת מתאימה לכל מורה שיש לו מרחב למידה.
- רק פעולות כלליות למרחבי Moodle של משרד החינוך.
- אין נתוני תלמידים גלויים.
- אין ציונים אישיים גלויים.
- אין צילומים מזויפים שמוצגים כאמיתיים.
- אם חסרה תמונה: מציגים placeholder ברור.

### חלק ב׳ — Moodle Teacher Hub / WWW

זהו הכלי הקיים בריפו `www`.  
מורה מוסיף אותו כקישור חיצוני / External Tool במרחב Moodle שלו.  
הקישור המרכזי הוא אחיד, והמערכת מזהה את המורה/קורס/מרחב לפי Moodle context.

עקרונות:

- אפליקציה מרכזית אחת.
- אין deployment נפרד לכל מורה.
- אין קוד נפרד לכל מורה.
- כל מורה רואה רק את המרחב שלו.
- כל קורס מופרד מקורס אחר.
- כל שיפור ב־`www` מתעדכן לכל המורים דרך אותו runtime.
- נתונים אמיתיים בלבד.
- אין דמו.
- אין נתונים מומצאים.
- Teacher Release נשאר **NO** עד שכל שערי האימות עוברים.

---

## מודל העבודה של הקישור האחיד למורים

```text
מורה פותח כלי חיצוני מתוך Moodle
        ↓
Moodle שולח LTI launch/context
        ↓
www מזהה מורה + מרחב + קורס
        ↓
המערכת מציגה מידע רק של אותו מרחב
        ↓
כל שיפור באפליקציה המרכזית מתעדכן לכולם דרך אותו קישור מרכזי
```

אין ליצור לכל מורה אפליקציה נפרדת.  
אין ליצור לכל מורה אתר נפרד.  
אין ליצור לכל מורה deployment נפרד.

---

## דרישות המצגת החיה למורים

המצגת צריכה להתחיל מהבסיס: פתיחת מרחב למידה.

פרקים ראשיים:

1. כניסה ופתיחת מרחב למידה.
2. היכרות עם מסך המרחב.
3. תלמידים ומשתתפים.
4. משימות והגשות.
5. ציונים.
6. דוחות ופעילות.
7. קישורים חיצוניים / External Tool.
8. שימוש ב־Moodle Teacher Hub / WWW.
9. פתרון תקלות נפוצות.
10. שאלות נפוצות של מורים.

סגנון כל שקופית:

```text
שאלה:
איך רואים ציונים של תלמיד במשימה מסוימת?

תשובה קצרה:
נכנסים לציונים, בוחרים את המשימה, ואז מחפשים את שם התלמיד.

מה ללחוץ:
1. ציונים
2. שם המשימה
3. שם התלמיד

צילום:
[צילום אמיתי מתוך Moodle]

טיפ:
אם לא רואים את המשימה, ייתכן שהיא מוסתרת או שלא פורסמה.
```

כל הטקסטים למורים צריכים להיות בעברית פשוטה, בלי עומס ובלי שפה טכנולוגית מיותרת.

---

## צילומים אמיתיים מתוך Moodle

המצגת חייבת להשתמש בצילומים אמיתיים מתוך מרחבי Moodle של משרד החינוך, בדיוק כפי שהמורה רואה אותם.

יש לבנות מערכת ניהול תמונות שתכלול:

- העלאת צילום מסך.
- שם פנימי.
- כותרת.
- תיאור מה רואים.
- שיוך לשאלה/שקופית/פרק.
- סימון אם יש מידע רגיש.
- סימון אם צריך טשטוש.
- סטטוס: טיוטה / לבדיקה / מאושר / לא לשימוש.

אסור לפרסם צילום עם מידע אישי גלוי של תלמידים, ציונים, מיילים, מזהים או פרטים פרטיים.

אם חסר צילום, להשתמש בטקסט:

```text
כאן ייכנס צילום אמיתי מתוך מרחב Moodle
```

---

## למידת כפתורי Moodle ומיפוי סביבת הלמידה

צריך לבנות תשתית למיפוי פעולות כלליות במרחבי Moodle של משרד החינוך:

- מסכים.
- כפתורים.
- תפריטים.
- דוחות.
- ציונים.
- תלמידים.
- משימות.
- הגשות.
- לוגים.
- הוספת קישור חיצוני.
- הגדרות כלליות.

המידע יגיע רק ממקורות מורשים:

- צילומי מסך שיניב מספק.
- הקלטות מסך שיניב מספק.
- קישורים שיניב מספק.
- תיאורים שיניב נותן.
- סביבת בדיקה מורשית בלבד.

אין לבצע scraping לא מורשה.  
אין לשמור credentials.  
אין לשמור tokens.

אם חסר מקור, המערכת תסמן:

```text
חסר מקור: יניב צריך לספק צילום/קישור/מיקום מדויק.
```

---

## אוטומציה מקסימלית לשליפת נתונים ב־WWW

בכל תחום שבו עובדים על Moodle Teacher Hub / WWW, המטרה היא אוטומציה מקסימלית במידת האפשר.

סדר עדיפות לשליפה:

1. LTI launch context.
2. LTI 1.3 claims.
3. NRPS — רשימת משתתפים, אם claims והרשאות קיימים.
4. AGS — ציונים/משימות, אם claims והרשאות קיימים.
5. Moodle Web Services API עם token אמיתי ומאומת.
6. קישורי ייצוא דוחות מתוך Moodle.
7. ייבוא דוחות אמיתיים:
   - Participants
   - Gradebook
   - Logs
   - Activity Completion / Course Structure
8. ניתוח קבצים שהמורה מעלה.
9. התאמת נתונים בין מקורות.

חובה:

- למקסם אוטומציה.
- לזהות אוטומטית מה זמין.
- להציג למורה מה חסר.
- להציע את הפעולה הבאה הכי פשוטה.
- לא להמציא נתונים.
- לא להציג אוטומציה מזויפת.

דוגמה ליכולת עתידית רצויה:

- כמה זמן מצטבר תלמיד תרגל בפרק זמן שהמורה בוחר, רק אם יש מקור נתונים אמיתי שמאפשר זאת.

---

## ממצאי דוח מרחב Moodle אמיתי של משרד החינוך — השלכות לפרויקט ולמדריך

מקור: בדיקה ישירה במרחב `moodlemoe.lms.education.gov.il` (קורס לדוגמה 1453, תפקיד מורה) בשילוב התיעוד הרשמי של Moodle. הדוח המלא הוזן ליניב־זיכרון של הפרויקט בתאריך 2026-07-07.

### 1. מה הדוח מוכיח על Moodle של משרד החינוך

- ליבת Moodle קיימת בכל מרחב־לימוד של משרד החינוך, ללא תלות בבית ספר, מחוז או מקצוע.
- כיכולות בסיס בכל מרחב: **דוחות יומני מעקב (Logs), דוח השתתפות, דוח השלמות פעילות, ויומן הציונים (Gradebook)** — כולל ייצוא לאקסל/CSV/ODS.
- תוספי משרד החינוך מוסיפים שכבת אנליטיקה ואוטומציה מעל הליבה:
  - **ניהול למידה** — לוח מחוונים אנליטי (KPIs + 9 גרפים) עם ייצוא SVG/PNG/CSV לכל גרף.
  - **דוח מסכם לבגרות — סמל שאלון** — סיכום ציונים לפי סמל שאלון (זמין רק במרחבים שהוגדר בהם סמל שאלון).
  - **ניהול קבוצות בפעילויות** — הגדרת מצב קבוצות לכל הפעילויות בבת אחת.
  - **תזכורות** (Reminders) — שליחת תזכורות אוטומטיות במייל לפני מועדי הגשה ואירועים.
- הפלטפורמה תומכת ב-LTI (External Tool) ובמסגרת Web Services מלאה עם token-ים והרשאות ברמת מנהל.

### 2. מקורות אמת ל־Teacher Hub

כל מקור להלן קיים במרחב אמיתי של משרד החינוך, ולכן מותר להסתמך עליו כמקור נתונים אמיתי (בכפוף להרשאות ולזמינות token):

| מקור | מטרה עבור Teacher Hub |
|------|----------------------|
| משתתפים (Participants) | רשימת תלמידי הכיתה, שיוך לקבוצות, גישה אחרונה, סטטוס |
| ציונים / Gradebook | ציונים אמיתיים, פריטי ציון, משוב, ייצוא Excel/CSV/XML |
| דוחות | פעילות מרחב, השתתפות, ניהול למידה |
| יומני מעקב / Logs | מקור אמת ל-Practice Time ולזיהוי דפוסי כניסה |
| יומני זמן־אמת | פעילות חיה כרגע (אם זמין למורה) |
| השלמות פעילות (Activity Completion) | מטריצת תלמיד־מול־פעילות עם סימוני השלמה |
| מבנה קורס / פעילויות | רשימת פעילויות, סוגים, מועדים, קבוצות |
| LTI / External Tool | זיהוי המורה והמרחב בכניסה לכלי |
| Web Services API | שליפת נתונים אוטומטית — דורש token מאומת + הרשאות |

### 3. החלטת מוצר מחייבת

Teacher Hub הוא **שכבת־על חכמה למורה מעל Moodle**, לא תחליף.  
הוא לא מחזיק תלמידים משלו, לא מנפיק ציונים משלו, לא מחליף את יומן הציונים.  
הוא **מרכז למורה במקום אחד** נתונים אמיתיים מתוך המקורות בטבלה שלעיל: משתתפים, ציונים, דוחות, לוגים, השלמות, פעילות וזמן תרגול — כפי שהם קיימים באותו רגע במרחב.

### 4. יכולת מרכזית: מדידת זמן תרגול

השאלה שהמורה צריך לענות עליה במהירות היא:

> "כמה זמן תלמיד X תרגל ב־24 השעות / 7 הימים / 30 הימים האחרונים, או בטווח תאריכים שאני בוחר?"

- המקור העיקרי הוא **יומני מעקב אמיתיים** (`imported_log_events`) מסונכרנים מ-Moodle Web Services (`core_report_get_log_data`).
- אסור להציג נתוני זמן תרגול על בסיס דמו, הערכה או השלמת פעילות בלבד.
- אם אין לוגים אמיתיים למרחב — יש להציג מצב BLOCKED מפורש בעברית ולא מספרים.

### 5. השלכות למדריך החכם העתידי (Guide Presentation)

מבנה הפרקים המחייב, לפי סדר שמורה חדש היה לומד:

1. כניסה למרחב Moodle
2. היכרות עם מסך המרחב
3. משתתפים
4. ציונים
5. דוחות
6. יומני מעקב
7. השלמות פעילות
8. ניהול למידה (תוסף משהח)
9. External Tool / LTI
10. שימוש ב-Teacher Hub
11. פתרון תקלות

לכל פרק יהיו בעתיד צילומי מסך אמיתיים מהמרחב שנבדק, ללא פרטי תלמידים גלויים (טשטוש חובה על שמות/ת"ז/מייל/ציונים אישיים).

### 6. השלכות לקוד הנוכחי — TODOs מעשיים בלבד

מיושם היום (אמת שאושרה בקוד):

- סנכרון משתתפים אמיתי (`core_enrol_get_enrolled_users`) → `imported_students`.
- סנכרון ציונים אמיתי (`gradereport_user_get_grade_items`) → `imported_grades` + `imported_grade_items`.
- סנכרון לוגים אמיתי (`core_report_get_log_data`) → `imported_log_events` (delete-then-insert לפי site+course).
- סנכרון השלמות אמיתי (`core_completion_get_activities_completion_status`) → `imported_task_completion`.
- Practice Time מבוסס לוגים אמיתיים עם מסננים 24h/7d/30d/מותאם + BLOCKED state.
- בידוד site+course בכל hook של ייבוא/שאילתה.
- מצב סנכרון חלקי (partial) עם `logsPartiallyFailed` / `completionsPartiallyFailed`.

חלקי (Roadmap):

- **דוח ניהול למידה** — התוסף הייחודי של משהח (KPIs + 9 גרפים) לא נשלף היום. אין לו endpoint רשמי ב-Web Services; דורש בירור אם קיים API של משהח או שנעבור דרך ייצוא CSV.
- **סמל שאלון / דוח בגרות** — לא זמין ב-Web Services הסטנדרטי. מסומן כ-Roadmap.
- **תזכורות אוטומטיות במייל** — קיים כתוסף במשהח. אנחנו לא מפעילים אותו; רק מציגים למורה אם הוא הוגדר.
- **פעילות בזמן־אמת** — אין תמיכה בסטרימינג ב-WS; אפשר לרענן לוגים ב-polling אם המורה מבקש.

BLOCKED (עד שיש token או הרשאה):

- Web Services שאינם חשופים למורה בלי שיתוף פעולה של מנהל המערכת של משהח.
- קריאה ישירה למסך "ניהול למידה" ללא CSV export ידני מהמורה.

אסור להציג ככפתור עובד:

- כל דוח שאין לו מקור נתונים אמיתי במסד — יוצג כמסך "עוד לא חובר לנתונים אמיתיים" בעברית ברורה, לא כפתור דמו.

### 7. אין להמציא יכולות

אם יכולת מופיעה בדוח אבל לא קיימת בקוד — היא **Roadmap** או **BLOCKED**, לא "עובדת". אין להציג במסך UI של Teacher Hub יכולת שלא נשלפת ממקור אמת בפועל.

### 8. סדר עבודה אחרי עדכון הזיכרון

1. עדכון PROJECT_MEMORY.md (הפרק הזה) — הושלם.
2. גזירת TODOs מדויקים מסעיף 6 (במידת הצורך למסכי `Guide` / `MoodleInstall` / `Reports` / `PracticeTimeSection`).
3. תיקוני UI קטנים ובטוחים בלבד, לאחר אישור נקודתי.
4. `npm run typecheck` + `npm run build`.
5. commit + push לענף הפעיל.

### 9. השלכות ניטור למחקר עתידי

- אם ומתי משהח יחשוף את "ניהול למידה" ב-API — יש לקשר אליו את דוחות ה-KPI ב-Teacher Hub.
- אם משהח יאפשר סמל שאלון ב-API — יש להוסיף דוח בגרות מסונכרן.
- כל שינוי בפלטפורמה של משהח יכול לשבור סנכרונים; לשמור לוג שינויים במסמך `STATE/` המתאים.

### 10. Audit יישור ריפו מול הדוח (2026-07-08)

לאחר קבלת הדוח המלא מ-Google Docs, בוצע audit רוחבי של הריפו מול העובדות בדוח. תוצאות:

**תואם ✅:**
- כל פונקציות ה-WS ב-`supabase/functions/moodle-sync/index.ts` הן שמות אמיתיים של Moodle Web Services (`core_enrol_get_enrolled_users`, `gradereport_user_get_grade_items`, `mod_assign_get_assignments`, `core_report_get_log_data`, `core_completion_get_activities_completion_status`).
- Footer של יניב + אינסטגרם קיים ב-`AppLayout.tsx` ומכסה את כל הראוטים למעט `/lti/launch` (bootstrap בלבד — מכוון).
- ראוט `/reports` משתמש ב-`ReportsCenter.tsx` (המימוש האמיתי).

**תוקן בסבב זה:**
- הוסרו 5 קבצי pages מיותמים (`Reports.tsx`, `Export.tsx`, `SettingsPage.tsx`, `ChapterDetail.tsx`, `Sites.tsx`) — אושר שאין אליהם import ב-src.
- Placeholder אנגלי בדוי ב-`Settings.tsx` (`moodle.yourcollege.edu.il`) הוחלף ב-`moodlemoe.lms.education.gov.il` האמיתי.
- Route `/dev/login` ב-`server.js` נחסם בפרודקשן (`NODE_ENV === "production"` מחזיר 404) — מונע דלת אחורית עם "מורה/מרחב לדוגמה".

**כלל מחייב:** לפני מחיקת קובץ — grep על ייבואים ב-src. הפעם הודגם דרך STATE/gemini-sync/git-diff-no-lock.patch שהקבצים כבר נוקו מ-App.tsx מזמן ורק הקבצים עצמם נשארו.

---

## ממצאי דוח התקנת כלים חיצוניים LTI במודל משהח — השלכות למוצר

מקור: דוח מקצועי על התקנת כלים חיצוניים LTI במודל של משרד החינוך (2026-07-08).

### 1. תפיסת LTI

- LTI (Learning Tools Interoperability) הוא תקן חיבור בין Moodle לבין כלים חיצוניים.
- **Moodle של משהח הוא ה-Platform. Teacher Hub / WWW הוא ה-Tool.**
- בזמן Launch, Moodle שולח בקשה חתומה עם: user, role, course context, activity context.
- הכלי חייב לאמת חתימה + session — לא להסתמך על URL רגיל בלבד.

### 2. גרסאות LTI

- **LTI 1.0 / 1.1** — נתמך אבל פחות מומלץ. מבוסס OAuth 1.0a.
- **LTI 1.3 / LTI Advantage** — היעד המקצועי. מבוסס OIDC/JWT. כולל:
  - **AGS** (Assignment and Grade Services) — כתיבת ציונים חזרה ל-Gradebook של Moodle.
  - **NRPS** (Names and Roles Provisioning Service) — שליפת רשימת משתתפים אמיתית.
  - **Deep Linking** — בחירת תוכן בזמן הוספת פעילות במרחב.

### 3. מסלול התקנת LTI במרחב Moodle

מסלול מדויק בהתאם לדוח:
`אפשרויות נוספות → כלי או שירות LTI חיצוני → Add tool`

מסך ה-LTI של המורה במודל משהח כולל:
- כפתור **Add tool**
- אזור **EduAppCenter** (קטלוג כלים מאושרים)
- טבלת כלים קיימים עם עמודות: שם, תיאור, Usage count, Show in activity chooser, פעולות/הגדרות

### 4. מצב "בהמתנה" — כלל מכריע ליישום

- כלי חדש עלול להישאר במצב **"בהמתנה"** עד אישור מנהל האתר של משהח.
- **כלי שמופיע ברשימת LTI של Moodle אינו בהכרח מחובר בהצלחה ל-WWW.**
- חיבור אמיתי דורש כל אלה:
  1. LTI launch תקין עם חתימה מאומתת.
  2. session תקף.
  3. site_id + course_id תקפים.
  4. permissions/sync מאומתים ב-Web Services (אם משתמשים).

**כלל UI מחייב:** אין להציג "מחובר ל-Moodle" רק על סמך רשימת LTI. להראות סטטוס חיבור אמיתי לפי 4 הבדיקות למעלה.

### 5. פרטיות ב-LTI

- LTI מסוגל להעביר: שם מלא, דוא"ל, תפקיד, ולעיתים ציונים.
- כללי מוצר:
  - שיתוף מינימום הכרחי (name, role, course_id בלבד ברירת מחדל).
  - חובה SSL.
  - עדיפות ל-LTI 1.3 מעל 1.1.
  - **לא לשמור** shared secret / client_secret / raw Moodle payload.

### 6. מצב יישום נוכחי

- קיים: `POST /lti/launch-1p1` ב-`src/server.js` (LTI 1.1). תואם לדוח.
- קיים: `src/pages/LtiBootstrap.tsx` — עמוד הכניסה. bootstrap בלבד, לא מציג פוטר — מכוון.
- Roadmap: מימוש LTI 1.3 מלא עם AGS/NRPS/Deep Linking.

---

## ממצאי דוח דוחות אקסל להורדה — השלכות Import Center ונתוני אמת

מקור: דוח מקצועי על הדוחות שהמורה יכול להוריד ממודל של משהח כקבצי Excel/CSV/JSON (2026-07-08).

### 1. מטרת הדוח

להוכיח שיש למורה **מסלול נתונים מעשי גם ללא Web Services ו-token** — דרך ייבוא קבצי export שהמורה מוריד ממודל ישירות.

### 2. חמישה מקורות הורדה עיקריים

| # | מקור | נתיב במודל |
|---|------|------------|
| 1 | יומן הציונים | ציונים → יצוא → Excel/ODS/CSV/XML |
| 2 | יומני מעקב (Logs) | דוחות → יומני מעקב → סינון → הורדה |
| 3 | דוח השלמות פעילות | דוחות → דוח השלמות פעילות → CSV UTF-8 |
| 4 | רשימת משתתפים | משתתפים → הורדת הטבלה |
| 5 | לוח "ניהול למידה" | דוחות → ניהול למידה → CSV/PNG/SVG לכל גרף |

### 3. פורמטים לפי מקור

- **יומן ציונים:** xlsx, ODS, CSV, XML. אפשרויות: בחירת פריטים, Course total, משוב, הסרת לא פעילים, פורמט ציון (מספרי/אחוז/אות), מספר ספרות, קבוצות.
- **יומני מעקב:** xlsx, CSV, ODS, HTML, JSON, PDF. מסננים: משתמש/תאריך/פעילות/פעולה/מקור/סוג אירוע.
- **דוח השלמות פעילות:** CSV UTF-8, CSV Excel.
- **משתתפים:** xlsx, CSV, ODS, HTML, JSON, PDF.
- **ניהול למידה:** לכל גרף — SVG/PNG (תמונה) + CSV (נתונים גולמיים).

### 4. עמודות מרכזיות (מבנה)

- **ציונים:** שם פרטי, שם משפחה, שם משתמש/ת"ז, דוא"ל, עמודה לכל פעילות/בוחן, עמודות משוב אופציונליות, Course total.
- **Logs:** זמן, שם מלא, משתמש מושפע, הקשר, רכיב, שם אירוע, תיאור, מקור (web/CLI/restore/web service), כתובת IP.
- **השלמות:** זיהוי תלמיד + עמודה לכל פעילות שמעקב השלמה מופעל בה.
- **משתתפים:** שם פרטי, שם משפחה, שם משתמש, דוא"ל, תפקידים, קבוצות, גישה אחרונה, מצב.

### 5. החלטת מוצר: Smart Import Center

כאשר Web Services/LTI claims לא מספיקים, המורה מעלה קובץ export אמיתי שהוריד ממודל. המערכת חייבת:

1. לזהות מאיזה דוח הקובץ הגיע (headers בעברית / באנגלית).
2. לזהות פורמט (CSV/xlsx/ODS/JSON).
3. לבדוק אילו עמודות קיימות.
4. לבדוק התאמה למודל של משהח.
5. לסמן מידע רגיש (ת"ז, דוא"ל, IP).
6. לשייך ל-site_id/course_id/session פעיל.
7. לבצע insert בטוח למסד רק אחרי אימות מלא.

### 6. מצב יישום נוכחי

- **קיים ומיושם:** `src/pages/Import.tsx` + `src/hooks/useImports.ts` + `src/lib/moodleImport.ts`. תומך ב-4 סוגי דוחות: students, grades, logs, activity completion. יש `parseMoodleFile` ו-`parsePastedTable` + זיהוי אוטומטי + preview + `postImport`.
- **חלקי:** אין עדיין זיהוי לדוח "ניהול למידה" (Roadmap — CSV per graph).
- **חסר:** UI ברור ב-ReportsCenter/MoodleInstall שמסביר מתי משתמשים ב-Import Center מול API אוטומטי.

### 7. סדר עדיפות נתונים (מעודכן)

1. LTI launch context
2. LTI 1.3 claims
3. NRPS (רשימת משתתפים)
4. AGS (ציונים)
5. Moodle Web Services API עם token מאומת
6. **קבצי export אמיתיים מהמורה** ← Import Center הקיים
7. ניתוח קבצים שהמורה מעלה (headers, columns)
8. התאמת נתונים בין מקורות

### 8. שלושת סטטוסי הנתונים המחייבים

כל UI שמראה נתונים חייב לסמן באחד משלושה בעברית ברורה:

- **"מסונכרן אוטומטית"** — API/WS/LTI פעיל ומחזיר נתונים.
- **"יובא מקובץ Moodle"** — המורה העלה קובץ export אמיתי דרך `/import`.
- **"חסום / נדרש מקור אמת"** — אין API ואין קובץ תקין. אין להציג נתונים.

### 9. כללי פרטיות בייבוא

- **לא לשמור** raw ת"ז/דוא"ל/IP כמידע גולמי בלוגים או בתיעוד.
- ב-DB לשמור רק מה שנדרש לתפעול (בדיקת בעלות + מיפוי לתלמיד/פעילות).
- IP: ברירת מחדל **null / masked / ignored**. לא לשמור אלא אם יש הצדקה מפורשת בכתב.
- **PDF הוא פורמט צפייה בלבד** — לא מקור import עיקרי. עדיפות ל-CSV/JSON/xlsx.

### 10. Roadmap — Import Center חכם

- הרחבת `moodleImport.ts` לזהות דוחות "ניהול למידה" לפי headers.
- זיהוי אוטומטי של דוחות בגרות (סמל שאלון) אם הפורמט קיים.
- Preview + סימון עמודות רגישות לפני insert.
- טיפול ב-headers בעברית ובאנגלית במקביל (Moodle של משהח יכול להיות בשתי השפות).
- באנר סטטוס `Import Center` בעמוד `/reports`: "יש לך קובץ ממודל? העלה ב-/import."

### 11. איסורים מוחלטים (חוזרים על כללי הריפו)

- אין ליצור fake import שנראה עובד.
- אין להוסיף כפתור פעיל אם אין backend אמיתי מאחוריו.
- אין להציג נתונים בלי שאחד משלושת הסטטוסים בסעיף 8 מוצג.
- אין לגעת ב-Teacher Release (נשאר NO).
- אין לשמור secrets / raw Moodle payload / IP גולמי.

---

## מערכת אנליטיקה פרטית ליניב בלבד

צריך לבנות מערכת אנליטיקה פרטית שרק יניב רואה.

מטרות:

- כמה נכנסו למצגת/אתר.
- כמה מבקרים ייחודיים, בלי חשיפת זהות אישית.
- אילו שקופיות נצפו הכי הרבה.
- כמה זמן נשארו באתר.
- כמה זמן נשארו בכל שקופית.
- מאיזה אזור בארץ בערך נכנסו, אם אפשר באופן חוקי ובטוח.
- סוג מכשיר: מחשב / טלפון / טאבלט.
- דפדפן.
- לחיצות על כפתורים.
- העתקת קישורים.
- פתיחת מדריך התקנה.

Routes מוצעים:

```text
/admin-hub/analytics
/guide/admin/analytics
```

כללים:

- האנליטיקה גלויה רק ליניב.
- אין חשיפת אנליטיקה למורים.
- אין שמירת סיסמאות.
- אין שמירת tokens.
- אין שמירת raw Moodle payload.
- אין מיקום מדויק מדי של אדם.
- עדיפות לעיר/אזור/מדינה בלבד.
- יש להפריד בין analytics של המצגת לבין analytics של Moodle Teacher Hub.

---

## Routes רצויים למערכת ההדרכה

```text
/guide
/guide/presentation
/guide/admin
/guide/slides
/guide/assets
/guide/moodle-map
/guide/share
/guide/install
```

## Routes רצויים לניהול WWW

```text
/admin-hub
/admin-hub/www-project
/admin-hub/teacher-install
/admin-hub/automation-status
/admin-hub/project-memory
/admin-hub/analytics
```

---

## טכנולוגיות מועדפות

Frontend:

- React
- TypeScript
- Vite
- Tailwind
- RTL מלא
- responsive מלא
- components נקיים ומודולריים

Backend:

- Node.js
- Express
- Supabase
- session isolation
- course/teacher scoping
- secure cookies

Moodle / LTI:

- LTI 1.1 OAuth verification
- LTI 1.3 OIDC/JWT אם קיים
- NRPS
- AGS
- Moodle Web Services
- export links
- import pipeline

Automation / QA:

- Playwright רק בסביבה מורשית
- GitHub Actions
- npm checks
- doctor scripts
- static audits
- live validations רק כאשר יש סביבת live מוכנה

---

## מה אסור לשבור

- LTI launch flow.
- Participants import.
- Gradebook import.
- Logs import.
- Supabase persistence.
- Render deployment.
- Teacher Release gate.
- נתיבים קיימים שעובדים.

אסור:

- Teacher Release YES.
- fake data.
- demo data.
- fake sync.
- secrets בריפו.
- raw student rows בתיעוד.
- raw Moodle payload בתיעוד.
- צילום עם מידע אישי גלוי.
- ערבוב בין Guide System לבין Teacher Hub System.

---

## תהליך עבודה מחייב

```text
דרישה חדשה מיניב
        ↓
עדכון PROJECT_MEMORY.md
        ↓
עדכון STATE/progress אם נדרש
        ↓
בדיקת השפעה על הארכיטקטורה
        ↓
שינוי קוד בענף חדש בלבד
        ↓
npm run check
        ↓
npm run typecheck
        ↓
npm run build
        ↓
npm run doctor אם קיים
        ↓
commit
        ↓
push
        ↓
deployment מתעדכן לפי החיבור הקיים
```

---

## מצב release

Teacher Release נשאר:

```text
NO
```

עד שיעברו כל שערי האימות:

- Web Services token אמיתי, אם משתמשים בו.
- קריאת API חיה מאומתת.
- NRPS/AGS claims מאומתים.
- Course Structure / Activity Completion מאומת.
- בידוד מורה/קורס מאומת.
- RLS enforcement מאומת.
- בדיקות live מתועדות.

---

## כלל סופי

המצגת היא מוצר הדרכה כללי לכל מורה.  
Moodle Teacher Hub / WWW הוא מוצר נתונים שמתחבר למרחב הספציפי של המורה.

שניהם נמצאים באתר הגדול, אבל הם לא אותו דבר.

המטרה היא לבנות מערכת אמיתית, חזקה, מתעדכנת, פשוטה למורים, עם אוטומציה מקסימלית ובלי לשבור את מה שכבר עובד.
