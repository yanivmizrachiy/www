# Hebrew No-Restart Marker — Moodle Teacher Hub

## Purpose

This document records the final Hebrew source-of-truth marker before Automation Core.

## Binding Hebrew rule

לא מתחילים מחדש.

ממשיכים מהקיים בלבד.

לא בונים דמו.

## Meaning

Future work must continue from the existing repository, existing app, active server `src/server.js`, active Tailwind config `tailwind.config.cjs`, and verified project state.

The next real code milestone remains Automation Core.
