# Remove Supabase Placeholder Fallback — Moodle Teacher Hub

Removed fake Supabase fallback values from the active frontend client.

The app must not silently connect to placeholder Supabase values.

Safety: no secrets, no deploy, no database change, no student data.
