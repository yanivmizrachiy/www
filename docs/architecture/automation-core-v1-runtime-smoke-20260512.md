# Automation Core V1 Runtime Smoke

This verifies that Automation Core V1 starts and responds locally.

Validated endpoints:

- `/health`
- `/api/sync/status`
- `/api/sync/run`

The sync output is aggregate-only and does not expose private student rows.
