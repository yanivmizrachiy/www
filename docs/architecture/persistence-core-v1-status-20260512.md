# Persistence Core V1 Status

Adds `/api/persistence/status`.

The endpoint truthfully reports whether the system has production persistence readiness or only local runtime store mode.

No secret values are returned. No student rows are returned.
