# Legacy Configs — Moodle Teacher Hub

`tailwind.config.cjs` is the active Tailwind config.

`tailwind.config.ts` was archived after audit showed the default Tailwind load matches the CJS config.

Do not restore without review, build check, and UI verification.
