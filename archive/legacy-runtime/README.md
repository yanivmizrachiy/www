# Legacy Runtime — Moodle Teacher Hub

`src/server.js` is the active runtime server.

`server.ts` was archived to reduce repository ambiguity after audit showed that package scripts use `src/server.js`, not `server.ts`.

Do not restore without review, build check, and STATE documentation.
